jQuery(function($) {
  var formId;
  formId = 'incomes_form';

  //Limpa o formulário
  $("#" + formId)[0].reset();
  $("#" + formId + " .tags").select2("val", "");
  
  //Atualiza o dashboard
  refreshDashboard();
});