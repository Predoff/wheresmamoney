$("#summary_content").html("<%= escape_javascript render(:partial => 'index') %>");

//coloca aba Recente como padrão
$('#summary_container li.by-month-tab a').click()

//coloca ambos como filtro padrão
$('input#show_option-both').click();