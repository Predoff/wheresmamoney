$("#summary_content").html("<%= escape_javascript render(:partial => 'index') %>");

$('#summary_container tr').each(function() {
  if ($(this).height() > 40) {
    return $(this).addClass('minimized');
  }
});