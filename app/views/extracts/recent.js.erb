//Carrega partial /extracts/recent/_index"
$("#extracts_content").html("<%= escape_javascript render(:partial => '/extracts/recent/index') %>");

//coloca ambos como filtro padrão
$("input:checked").click();